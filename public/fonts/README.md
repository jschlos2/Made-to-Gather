# Local fonts

Place licensed `.woff2` font files here, then update the `@font-face` rules near the top of `src/styles/global.css`. The starter uses system font fallbacks and makes no external font requests.
